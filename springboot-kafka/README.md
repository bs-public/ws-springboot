### Spin the Zookeeper and Kafka by using the docker compose file

### Commands 
Execute the commands from devops folder.

To run the container 
	
	docker compose up -d

To stop the container
	
	docker compose down
